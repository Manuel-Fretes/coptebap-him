import 'package:flutter/material.dart';
class finanzas extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('FINANZAS', style: TextStyle (fontSize: 40.0),),
    );
  }
}